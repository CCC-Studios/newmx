<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.NewMx" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/newmx_red</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/newmx_red_light</item>
        <item name="android:statusBarColor">@color/newmx_red_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Translucent theme for PROCESS_TEXT and share-target activities.
         They don't show UI — they do the work and finish immediately. -->
    <style name="Theme.NewMx.Translucent" parent="Theme.NewMx">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
